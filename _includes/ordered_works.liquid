{% comment %}
  Build ordered works from _data/works_order.yml (Pages CMS shape: order: [{ slug: "..." }]).
  Preserves CMS order, appends works not yet in the file, skips unknown slugs.
{% endcomment %}
{% assign ordered_works = "" | split: "" %}
{% assign order_list = site.data.works_order.order %}

{% if order_list and order_list.size > 0 %}
  {% for item in order_list %}
    {% assign work = site.works | where: "slug", item.slug | first %}
    {% if work %}
      {% assign ordered_works = ordered_works | push: work %}
    {% endif %}
  {% endfor %}
  {% for work in site.works %}
    {% assign in_order = false %}
    {% for item in order_list %}
      {% if item.slug == work.slug %}
        {% assign in_order = true %}
      {% endif %}
    {% endfor %}
    {% unless in_order %}
      {% assign ordered_works = ordered_works | push: work %}
    {% endunless %}
  {% endfor %}
{% else %}
  {% assign ordered_works = site.works %}
{% endif %}
